<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-20">
    <div class="w-full flex justify-end">
      <a class="bg-red-500 text-sm rounded-md px-3 py-2 mb-5 inline-block" href="/logout">Logout</a>
    </div>
    <h5>Edit Your Post.</h5>
    <form action="/update/<%= post._id%>" method="post">
      <textarea placeholder="write your heart out" class="resize-none w-1/3 p-3 bg-transparent border-2 border-zinc-800 rounded-md outline-none" name="content" id=""><%=post.content%></textarea>
      <input  class="px-3 w-40 py-3 bg-yellow-500 text-black block rounded-md mt-2 text-sm" type="submit" value="Update Post">
    </form>
  </div>
</body>
</html>